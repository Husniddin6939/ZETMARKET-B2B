export interface InavigationType {
  path: string;
  title: string;
  icon: string;
  role: string[];
}

export type TCardPropsType = {
  icon: string;
  title: String;
  value: Number | String;
  color: String;
};
